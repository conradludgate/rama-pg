[package]
name = "rama-pg"
version = "0.1.0"
edition = "2024"

[dependencies]
rama = { version = "=0.2.0", features = ["tcp", "rustls"] }
tokio = { version = "1", features = ["full"] }
bytes = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
